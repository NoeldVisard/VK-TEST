{% extends 'base.html.twig' %}

{% block title %}Hello LabyrinthController!{% endblock %}

{% block body %}
    В каждое поле для ввода введите 10 чисел через пробел, обозначающих кол-во ходов
    необходимых для прохода через клетку, где 0 - стена. (например: «0 1 0 0 0 0 1 3 2 0»)
    <form action="{{ path('labyrinth_action') }}" method="post" >
        {% for i in range(0, 9) %}
            <div>
                <label>
                {{ loop.index0 }} <input name="{{ loop.index0 }}" type="text">
                </label>
            </div>
        {% endfor %}
        <div>
            <label> Точка старта (напр. 0 1)
                <input name="start" type="text">
            </label>
        </div>
        <div>
            <label> Точка финиша (напр. 0 9)
                <input name="finish" type="text">
            </label>
        </div>
        <input type="submit">
    </form>

    <style>
        @import url('https://fonts.googleapis.com/css?family=Open+Sans:400,700');

        body {
            font-family: 'Open Sans', sans-serif;
            font-size: 16px;
            font-weight: 400;
        }
    </style>
{% endblock %}
